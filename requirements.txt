Flask>=3.0,<4
openpyxl>=3.1
reportlab>=4.0
python-dateutil>=2.8
Pillow>=10.0
waitress>=3.0

# Optional: AI equipment-label reading. The scanner falls back to on-device
# OCR without it, so the app is fully usable if you leave this out.
google-generativeai>=0.8 ; python_version >= "3.9"
